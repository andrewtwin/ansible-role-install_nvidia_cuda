---
- name: Install cude-keyring package
  tags: ubuntu
  ansible.builtin.apt:
    deb: "https://developer.download.nvidia.com/compute/cuda/repos/{{ distro }}/{{ os_arch }}/cuda-keyring_1.1-1_all.deb"
    state: present
    allow_unauthenticated: True 

- name: Update apt cache
  tags: ubuntu
  ansible.builtin.apt:
    update_cache: True

- name: Install the packages
  tags: ubuntu
  notify: Reboot
  loop: "{{ package_list }}"
  ansible.builtin.apt:
    name: "{{ item }}"
    state: present
    autoremove: "{{ allow_autoremove }}" 

- name: Disable the nouveau drivers
  tags: rhel
  notify:
  - Update initramfs
  - Reboot
  ansible.builtin.template:
    src: templates/modprobe.j2  
    dest: /etc/modprobe.d/blacklist-nouveau.conf
    owner: root
    group: root
    mode: "644"
